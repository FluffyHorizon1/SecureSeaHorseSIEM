# =============================================================================
# SecureSeaHorse Client Configuration — v1.2.0 (Phase 1)
# =============================================================================

# --- Connection ---
server_ip   = 127.0.0.1
port        = 65432
device_id   = 7001

# --- mTLS Certificates ---
ca_path     = certs/ca.crt
client_crt  = certs/client.crt
client_key  = certs/client.key

# --- Logging ---
log_file        = client.log

# --- [Phase 1] Log Rotation ---
log_max_bytes   = 10485760    # 10 MB before rotation
log_max_files   = 5           # Keep client.log.1 through client.log.5

# --- [Phase 1] Exponential Backoff ---
backoff_base_ms = 1000        # Initial retry delay (1 second)
backoff_max_ms  = 60000       # Maximum retry delay cap (60 seconds)
